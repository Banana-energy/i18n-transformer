{
  "name": "@karpo/i18n-transformer",
  "version": "1.0.0",
  "description": "An automatic i18n conversion plugin.",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clear && rollup -c",
    "release:shared": "pnpm -r --filter=./packages/** run clear && pnpm -r --filter=./packages/shared run release && rollup -c",
    "release:vite-plugin": "pnpm -r --filter=./packages/** run clear && pnpm -r --filter=./packages/vite-plugin run release && rollup -c",
    "release:webpack-plugin": "pnpm -r --filter=./packages/** run clear && pnpm -r --filter=./packages/webpack-plugin run release && rollup -c",
    "clear": "pnpm -r --filter=./packages/** run clear"
  },
  "keywords": [
    "vite-plugin",
    "rollup-plugin",
    "i18n",
    "auto-i18n"
  ],
  "author": "Banana-energy",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.11.1",
    "@types/node": "^22.7.2",
    "eslint": "^8.57.1",
    "eslint-plugin-import-x": "^4.3.1",
    "globals": "^15.9.0",
    "rimraf": "^6.0.1",
    "rollup": "^4.22.5",
    "rollup-plugin-ts": "^3.4.5",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.7.0"
  }
}
